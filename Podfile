platform :ios, '9.0'

target 'Todoey' do
  use_frameworks!

  pod 'RealmSwift'
  pod 'SwipeCellKit'
  
  # the pod itself, then the repo of that pod, then the branch we wanna use from that repo
  pod 'ChameleonFramework/Swift', :git => 'https://github.com/wowansm/Chameleon.git', :branch => 'swift5'

end
